/*
 * EJERCICIO 24A: FizzBuzz (Clásico de entrevistas)
 * 
 * TAREA:
 * Imprime los números del 1 al n, pero:
 * - Si es divisible por 3 → imprime "Fizz"
 * - Si es divisible por 5 → imprime "Buzz"
 * - Si es divisible por ambos → imprime "FizzBuzz"
 * - Si no → imprime el número
 * 
 * Implementa 3 versiones:
 * 1. Con if/else clásico
 * 2. Sin usar operador % (pista: usa contadores)
 * 3. Retornando un vector<string> en vez de imprimir
 * 
 * EJEMPLO DE SALIDA (n=20):
 * 1, 2, Fizz, 4, Buzz, Fizz, 7, 8, Fizz, Buzz, 
 * 11, Fizz, 13, 14, FizzBuzz, 16, 17, Fizz, 19, Buzz
 * 
 * BONUS:
 * Extiéndelo: si es divisible por 7 → "Bazz"
 * Entonces divisible por 3 y 7 → "FizzBazz"
 * Divisible por 5 y 7 → "BuzzBazz"
 * Divisible por 3, 5 y 7 → "FizzBuzzBazz"
 */

// Tu código aquí
