/*
 * EJERCICIO 16D: Aplicador de Funciones (Compuesto: Templates + Lambdas + POO)
 *
 * TAREA:
 * - Escribe una funcion Template generica `aplicar_filtro<Contenedor, Predicado>`.
 * - Esta funcion toma un contenedor STL y un predicado (lambda) y devuelve un vector con
 *   todos los elementos que cumplan la condicion.
 * - Pruébala con un std::vector<int>, std::list<double>, y un std::vector<ObjetoPOO>.
 */
