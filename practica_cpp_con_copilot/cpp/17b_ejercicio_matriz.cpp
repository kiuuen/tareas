/*
 * EJERCICIO 17B: Sobrecarga Matriz 2x2 (Isolado)
 *
 * TAREA:
 * - Crea la clase Matriz2x2 con un arreglo 2D interno.
 * - Sobrecarga el operador + y - (Suma y resta de componentes).
 * - Opcional avanzado: Sobrecarga el operador * (Multiplicacion fila por columna).
 * - Sobrecarga el operador << (std::ostream) para imprimir la grilla facilmente.
 */
