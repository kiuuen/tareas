/*
 * EJERCICIO 17C: Ordenamiento Nativo (Compuesto: Sobrecarga + STL Algorithm)
 *
 * TAREA:
 * - Crea una clase 'Jugador' con atributo 'nivel_habilidad'.
 * - Sobrecarga el operador < en Jugador para que devuelva el menor nivel_habilidad.
 * - Sobrecarga el operador == para ver si empatan.
 * - Llama a std::sort() pasandole solo .begin() y .end() de un vector<Jugador>. 
 * - Veras que std::sort funciona solo gracias a tu sobrecarga interna del operador '<'!
 */
